{
  "name": "mockgpio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "export NODE_ENV=test || SET \"NODE_ENV=test\" && node index.js",
    "develop": "export NODE_ENV=develop || SET \"NODE_ENV=develop\" && node index.js",
    "start": "node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pigpio": "^1.1.4"
  },
  "devDependencies": {
    "pigpio-mock": "0.0.1"
  }
}
